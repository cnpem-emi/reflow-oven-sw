#include "mbed.h"=undefined
#include <string>=undefined
#include <SPI.h> =undefined
#include <Adafruit_GFX.h>  =undefined
#include <Adafruit_PCD8544.h> =undefined
#include <SPI.h>=undefined
#include "NOKIA_5110.h"=undefined
// main() runs in its own thread in the OS=undefined
float variable; =undefined
DigitalIn pb(p14); //push botton=undefined
const char arrow[5] = {0x41, 0x22, 0x14, 0x08, 0x00};
int main()=undefined
{=undefined
    char *test = (char *)malloc(16);
    snprintf(test, 16, "test 2");=undefined
    // Init the data structures and NokiaLcd class=undefined
    LcdPins myPins;=undefined
    myPins.sce  = p8;
    myPins.rst  = p9;
    myPins.dc   = p10;
    myPins.mosi = p11;
    myPins.miso = NC;
    myPins.sclk = p13;
    =undefined
    NokiaLcd myLcd( myPins );=undefined
    // Start the LCD=undefined
    myLcd.InitLcd();=undefined
    variable = 0;
 =undefined
    // Draw a test pattern on the LCD and stall for 15 seconds=undefined
    myLcd.DrawString("my friend");=undefined
    for(int i = 0; i < 10; i++) {
    snprintf(test, 16, "test %d", i);=undefined
    myLcd.SetXY(0,0);=undefined
    myLcd.DrawString(test);=undefined
    for(int j = 0; j < 6;j++){
        myLcd.SendDrawData(arrow[j]);=undefined
    }=undefined
    wait_us( 2000000 );=undefined
    // Turn off the LCD and enter an endless loop=undefined
    while (1){=undefined
}=undefined
       =undefined
TARGET=LPC1768
TARGET_CODE=1010
